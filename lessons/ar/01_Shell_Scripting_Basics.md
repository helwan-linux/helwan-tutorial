---

# Shell Scripting Basics (Arabic)

البرمجة النصية في الشل طريقة قوية لأتمتة المهام المتكررة في لينكس عن طريق كتابة تسلسل من الأوامر في ملف يمكن للشل تنفيذه.

## المتغيرات

تُستخدم المتغيرات لتخزين البيانات التي يمكن إعادة استخدامها في جميع أنحاء السكريبت. يتم تعيين القيم بدون مسافات حول علامة =:

NAME="Helwan"
AGE=25

للوصول إلى قيمة متغير، ضع \$ قبله:

echo "مرحبًا، \$NAME! عمرك \$AGE سنة."

## الجمل الشرطية

تتيح الجمل الشرطية اتخاذ قرارات في السكريبت.

مثال على جملة if:

if \[ \$AGE -ge 18 ]; then
echo "أنت بالغ."
else
echo "أنت قاصر."
fi

الأقواس \[ ] هي مرادف لأمر test. يمكنك استخدام مشغلات مثل:

* -eq (يساوي)
* -ne (لا يساوي)
* -lt (أقل من)
* -le (أقل أو يساوي)
* -gt (أكبر من)
* -ge (أكبر أو يساوي)

## الحلقات

تساعدك الحلقات على تنفيذ الأوامر عدة مرات.

مثال على حلقة for:

for i in 1 2 3 4 5
do
echo "الرقم \$i"
done

مثال على حلقة while:

count=1
while \[ \$count -le 5 ]
do
echo "العد \$count"
((count++))
done

## الدوال

الدوال هي كتل قابلة لإعادة الاستخدام من الكود.

تعريف دالة:

greet() {
echo "مرحبًا، \$1!"
}

استدعاء الدالة مع تمرير قيمة:

greet "المستخدم"

الـ \$1 تمثل أول قيمة يتم تمريرها إلى الدالة.

## التعليقات

تبدأ التعليقات بـ # ويتم تجاهلها من قبل الشل. استخدم التعليقات لشرح كودك.

# هذا تعليق

echo "مرحبًا بالعالم"  # هذا يطبع رسالة

---

## 🧪 أوامر مفيدة (Aliases)

```bash
alias sync="sudo pacman -Syyy"
alias install="sudo pacman -S"
alias update="sudo pacman -Syyu"
alias search="sudo pacman -Ss"
alias search-local="sudo pacman -Qs"
alias pkg-info="sudo pacman -Qi"
alias local-install="sudo pacman -U"
alias helwan="uname -a"
```
